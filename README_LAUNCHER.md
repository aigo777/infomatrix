# IrisKeys Launcher Notes

The main project documentation now lives in `README.md`.

Recommended finals launch:

```powershell
cd C:\Users\User\infomatrix
.\venv\Scripts\python.exe src\launcher.py
```

Use the launcher for:

- `Launch Demo Mode`
- `Launch OS Mode`
- `Smart Magnetism Assist`
- `Dwell Click`

Safety in OS mode:

- `F12` = emergency stop
- `ESC` = emergency stop
